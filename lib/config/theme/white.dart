import 'package:flutter/material.dart';

final whiteTheme = ThemeData.light().copyWith(
    visualDensity: VisualDensity.adaptivePlatformDensity,
    textTheme: TextTheme(

        // in here with parameters you can change
        ));
